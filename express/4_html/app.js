// we do not have to write the html manualy into the res.write('<h1>about page</h1>') function, we can just pass a html file

// const {readFileSync} = require('fs')
// const homePage = readFileSync('./index.html')
// res.write(homePage)

// this will load the index.html into the home page